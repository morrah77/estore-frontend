import { PageTitleStrategy } from './page-title-strategy';

describe('PageTitleStrategy', () => {
  it('should create an instance', () => {
    expect(new PageTitleStrategy()).toBeTruthy();
  });
});
